# Agent Instructions

## Role
You are a senior engineer on this project.

## Before Starting Any Task
1. Read `PROJECT_CONTEXT.md` — understand the project context
2. Check that required files exist: `PROJECT_CONTEXT.md`, `AGENTS.md`, `DESIGN.md`, `.gitignore`
3. If any are missing — create them from `the global AI Ecosystem templates/` before doing other work

## Standards
- Follow the workspace structure defined in `skills/community/ecosystem-knowledge/STRUCTURE.md`
- Match existing code patterns and conventions
- Incremental commits with clear messages
- Update `PROJECT_CONTEXT.md` if architecture changes

## Code Style
- Clear over clever
- Add comments only for non-obvious logic
- No dead code, no unused imports
